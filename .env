# ================================
# Sistema de Gestão de Protocolos
# Arquivo de Configuração
# ================================

# ============ BANCO DE DADOS ============
# URL de conexão do MongoDB
# Para MongoDB Local (recomendado para desenvolvimento):
MONGO_URL=mongodb://localhost:27017/

# Para MongoDB Atlas (cloud), substitua com sua string de conexão:
# MONGO_URL=mongodb+srv://username:password@cluster.mongodb.net/

# Nome do banco de dados
DB_NAME=protocolos_db

# ============ SEGURANÇA - ADMIN ============
# OBRIGATÓRIO: Configure estas credenciais antes do primeiro uso!
# Estas credenciais serão usadas APENAS na primeira inicialização

# Nome de usuário do administrador (padrão: admin)
ADMIN_USER=admin

# Senha do administrador - ALTERE ESTA SENHA!
# Requisitos: mínimo 8 caracteres, pelo menos 1 número e 1 letra
# IMPORTANTE: Altere imediatamente após o primeiro login!
ADMIN_PASSWORD=admin123@

# ============ SEGURANÇA - SENHA ============
# Número de iterações PBKDF2 para hashing de senha (padrão: 260000)
PBKDF2_ITER=260000

# ============ SEGURANÇA - JWT ============
# Chaves secretas para JWT e CSRF (já geradas)
# IMPORTANTE: Em produção, gere novas chaves únicas!
# Comando: python -c "import secrets; print(secrets.token_urlsafe(32))"

# Chave secreta para assinar tokens JWT
JWT_SECRET_KEY=WUoizhB7HXpw1L4TSES-qjTKNAYtzhxo0PueKVd_7OE

# Chave secreta para tokens CSRF
CSRF_SECRET_KEY=vvHrCsazYcSpvDeSHCCeavzMQH_pgWQk3er7kbekhGg

# Tempo de expiração do access token em minutos (padrão: 60)
JWT_ACCESS_TOKEN_EXPIRE_MINUTES=60

# Tempo de expiração do refresh token em dias (padrão: 7)
JWT_REFRESH_TOKEN_EXPIRE_DAYS=7

# ============ SEGURANÇA - LOGIN ============
# Número máximo de tentativas de login antes de bloquear (padrão: 5)
LOGIN_MAX_ATTEMPTS=5

# ============ NEGÓCIO ============
# Número de dias úteis para considerar protocolo atrasado (padrão: 30)
BUSINESS_DAYS_THRESHOLD=30

# ============ SERVIDOR ============
# Host do servidor (padrão: 0.0.0.0 para aceitar todas conexões)
# Use 127.0.0.1 para aceitar apenas conexões locais
HOST=0.0.0.0

# Porta do servidor (padrão: 8000)
PORT=8000

# ============ NOTAS DE SEGURANÇA ============
# 1. NUNCA commite este arquivo no Git
# 2. Use senhas fortes em produção
# 3. Gere novas chaves JWT/CSRF únicas para cada instalação
# 4. Altere a senha do admin após o primeiro login
# 5. Em produção, use HTTPS e configure firewall
