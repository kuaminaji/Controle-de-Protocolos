# ================================
# Sistema de Gestão de Protocolos
# Variáveis de Ambiente - Exemplo
# ================================

# IMPORTANTE: Copie este arquivo para .env e configure com seus valores
# Nunca commite o arquivo .env com credenciais reais!

# ============ BANCO DE DADOS ============
# Tipo de banco de dados: "mongodb" ou "sqlite" (padrão: sqlite)
DB_TYPE=sqlite

# --- SQLite Configuration (quando DB_TYPE=sqlite) ---
# Caminho para o arquivo do banco de dados SQLite
SQLITE_DB_PATH=protocolos.db

# --- MongoDB Configuration (quando DB_TYPE=mongodb) ---
# URL de conexão do MongoDB
# Exemplos:
#   Local: mongodb://localhost:27017/
#   Atlas: mongodb+srv://usuario:senha@cluster.mongodb.net/
MONGO_URL=mongodb://localhost:27017/

# Nome do banco de dados MongoDB
DB_NAME=protocolos_db

# ============ SEGURANÇA - ADMIN ============
# OBRIGATÓRIO: Credenciais do usuário administrador inicial
# Estas credenciais são usadas apenas na primeira inicialização
# quando nenhum usuário existe no banco de dados

# Nome de usuário do admin (padrão: admin)
ADMIN_USER=admin

# Senha do admin - OBRIGATÓRIA e deve ser forte!
# Requisitos mínimos:
# - Mínimo 8 caracteres
# - Pelo menos 1 número
# - Pelo menos 1 letra
# ALTERE esta senha imediatamente após primeiro login!
ADMIN_PASSWORD=admin123@

# ============ SEGURANÇA - SENHA ============
# Número de iterações PBKDF2 para hashing de senha (padrão: 260000)
# Valores maiores = mais seguro, mas mais lento
PBKDF2_ITER=260000

# ============ SEGURANÇA - JWT ============
# OBRIGATÓRIO: Chaves secretas para JWT e CSRF
# Gere com: python -c "import secrets; print(secrets.token_urlsafe(32))"
# NUNCA compartilhe essas chaves ou commite no Git!

# Chave secreta para assinar tokens JWT
JWT_SECRET_KEY=

# Chave secreta para tokens CSRF
CSRF_SECRET_KEY=

# Tempo de expiração do access token em minutos (padrão: 60)
JWT_ACCESS_TOKEN_EXPIRE_MINUTES=60

# Tempo de expiração do refresh token em dias (padrão: 7)
JWT_REFRESH_TOKEN_EXPIRE_DAYS=7

# ============ SEGURANÇA - LOGIN ============
# Número máximo de tentativas de login antes de bloquear (padrão: 5)
LOGIN_MAX_ATTEMPTS=5

# ============ NEGÓCIO ============
# Número de dias úteis para considerar protocolo atrasado (padrão: 30)
BUSINESS_DAYS_THRESHOLD=30

# ============ SERVIDOR ============
# Host do servidor (padrão: 0.0.0.0 para aceitar todas conexões)
# Use 127.0.0.1 para aceitar apenas conexões locais
HOST=0.0.0.0

# Porta do servidor (padrão: 8000)
PORT=8000

# ============ PRODUÇÃO ============
# Em produção, configure também:
# - HTTPS/TLS com certificados válidos
# - Firewall para restringir acesso
# - Backups automáticos criptografados
# - Logs centralizados e monitoramento
# - Redis para rate limiting persistente

# Exemplos de configuração avançada (descomente se necessário):
# REDIS_URL=redis://localhost:6379
# LOG_LEVEL=INFO
# ENABLE_CORS=false
# ALLOWED_ORIGINS=https://seu-dominio.com.br
